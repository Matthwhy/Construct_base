<odoo>
    <data noupdate="1">

        <!-- Regra de acesso para project.task.material -->
        <record id="project_task_material_internal_rule" model="ir.rule">
            <field name="name">Project Task Material Internal Rule</field>
            <field name="model_id" ref="model_project_task_material"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
        </record>

        <!-- Regra de acesso para project.task.equipment -->
        <record id="project_task_equipment_internal_rule" model="ir.rule">
            <field name="name">Task Equipment Internal Rule</field>
            <field name="model_id" ref="model_project_task_equipment"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
        </record>

    </data>
</odoo>
